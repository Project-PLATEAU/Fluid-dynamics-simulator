<?php

namespace App\Commons;


/**
 * 定数定義クラス
 * ※view bladeで直接呼び出すため、「\Eloquent」クラスを継承する必要があります。
 */
class Constants extends \Eloquent
{
    /**
     * ========================================================
     * クッキー名
     * ========================================================
     */
    const LOGIN_COOKIE_NAME = "BLADE_CDF_LOGIN_USER";

    /**
     * ========================================================
     * クッキーの有効期限: 1日 (24*60*60)秒
     * ========================================================
     */
    const LOGIN_COOKIE_EXPIRATION = 86400;
    /**
     * ========================================================
     * 最大表示行数: 100
     * ========================================================
     */
    const SELECT_LIMIT = 100;

    /**
     * ========================================================
     * モデル種別表示設定：空欄表示(例：ホーム画面)
     * ========================================================
     */
    const MODEL_KIND_BLANK = "";
    /**
     * ========================================================
     * モデル種別表示設定：3D都市モデル関連画面
     * ========================================================
     */
    const MODEL_KIND_CITY = "3D都市モデル";
    /**
     * ========================================================
     * モデル種別表示設定：シミュレーション関連画面
     * ========================================================
     */
    const MODEL_KIND_SIMULATION = "シミュレーションモデル";
    /**
     * ========================================================
     * モデル種別表示設定：熱流体解析ソルバ関連画面
     * ========================================================
     */
    const MODEL_KIND_SOLVER = "熱流体解析ソルバ";

    /**
     * ========================================================
     * モデル識別名表示：空欄表示
     * ========================================================
     */
    const MODEL_IDENTIFICATE_NAME_DISPLAY_BLANK = "";
    /**
     * ========================================================
     * モデル識別名表示：新規追加
     * ========================================================
     */
    const MODEL_IDENTIFICATE_NAME_DISPLAY_ADD_NEW = "新規登録";

    /**
     * ========================================================
     * 実行ステータスコード：未
     * ========================================================
     */
    const RUN_STATUS_CODE_NONE = 0;
    /**
     * ========================================================
     * 実行ステータスコード：開始処理中
     * ========================================================
     */
    const RUN_STATUS_CODE_START_PROCESSING = 1;
    /**
     * ========================================================
     * 実行ステータスコード：実行中
     * ========================================================
     */
    const RUN_STATUS_CODE_RUNNING = 2;
    /**
     * ========================================================
     * 実行ステータスコード：正常終了
     * ========================================================
     */
    const RUN_STATUS_CODE_NORMAL_END = 3;
    /**
     * ========================================================
     * 実行ステータスコード：異常終了
     * ========================================================
     */
    const RUN_STATUS_CODE_ABNORMALITY_END = 4;
    /**
     * ========================================================
     * 実行ステータスコード：中止処理中
     * ========================================================
     */
    const RUN_STATUS_CODE_CANCEL_PROCESSING = 5;
    /**
     * ========================================================
     * 実行ステータスコード：中止
     * ========================================================
     */
    const RUN_STATUS_CODE_CANCEL = 6;
    /**
     * ========================================================
     * 実行ステータスコード：管理者中止
     * ========================================================
     */
    const RUN_STATUS_CODE_ADMIN_CANCEL = 7;

    /**
     * ========================================================
     * 実行ステータス名：未
     * ========================================================
     */
    const RUN_STATUS_NONE = "未";
    /**
     * ========================================================
     * 実行ステータス名：開始処理中
     * ========================================================
     */
    const RUN_STATUS_START_PROCESSING = "開始処理中";
    /**
     * ========================================================
     * 実行ステータス名：実行中
     * ========================================================
     */
    const RUN_STATUS_RUNNING = "実行中";
    /**
     * ========================================================
     * 実行ステータス名：正常終了
     * ========================================================
     */
    const RUN_STATUS_NORMAL_END = "正常終了";
    /**
     * ========================================================
     * 実行ステータス名：異常終了
     * ========================================================
     */
    const RUN_STATUS_ABNORMALITY_END = "異常終了";
    /**
     * ========================================================
     * 実行ステータス名：中止処理中
     * ========================================================
     */
    const RUN_STATUS_CANCEL_PROCESSING = "中止処理中";
    /**
     * ========================================================
     * 実行ステータス名：中止
     * ========================================================
     */
    const RUN_STATUS_CANCEL = "中止";
    /**
     * ========================================================
     * 実行ステータス名：管理者中止
     * ========================================================
     */
    const RUN_STATUS_ADMIN_CANCEL = "管理者中止";

    /**
     * ========================================================
     * 3D都市モデル一覧画面から共有画面に遷移するパターン
     * ========================================================
     */
    const SHARE_MODE_CITY_MODEL = 1;
    /**
     * ========================================================
     * シミュレーションモデル一覧画面から共有画面に遷移するパターン
     * ========================================================
     */
    const SHARE_MODE_SIMULATION_MODEL = 2;

    /**
     * ========================================================
     * ソルバーの公開状況：公開
     * ========================================================
     */
    const SOLVER_PUBLISH = "公開";
    /**
     * ========================================================
     * ソルバーの公開状況：非公開
     * ========================================================
     */
    const SOLVER_NON_PUBLISH = "非公開";

    /**
     * ========================================================
     * 可視化種別：1.風況
     * ========================================================
     */
    const VISUALIZATION_TYPE_WINDY = 1;
    /**
     * ========================================================
     * 可視化種別：2.温度
     * ========================================================
     */
    const VISUALIZATION_TYPE_TEMP = 2;
    /**
     * ========================================================
     * 可視化種別：3.暑さ指数
     * ========================================================
     */
    const VISUALIZATION_TYPE_HEAT_INDEX = 3;

    /**
     * ========================================================
     * シミュレーションモデル編集画面
     *  実施施策一覧の操作(追加・削除)に使用するセッションキー
     * ========================================================
     */
    const SM_POLICY_SESSION_KEY = "smPolicies";

    /**
     * ========================================================
     * シミュレーションモデルの公開状況（有効）：公開
     * ========================================================
     */
    const SIMULATION_MODEL_PUBLISH = "公開中";
    /**
     * ========================================================
     * シミュレーションモデルの公開状況（無効）：未
     * ========================================================
     */
    const SIMULATION_MODEL_NON_PUBLISH = "未";
}
